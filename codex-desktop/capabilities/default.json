{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default capability for the main Codex Desktop window. Minimal on purpose — expand only as real product features (Project switching, file dialogs, etc.) need specific permissions.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:default",
    "updater:default"
  ]
}
